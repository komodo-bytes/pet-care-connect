{
  "name": "pet-care-connect-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "react-scripts start",
    "build": "npm run ensure-permissions && react-scripts build",
    "ensure-permissions": "node ./ensure-permissions.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  
  "dependencies": {
    "axios": "^1.7.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.26.2",
    "react-scripts": "5.0.1"
  },
  
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}